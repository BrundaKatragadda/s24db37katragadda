extends layout

block content
  h1= title
  p Bonjour,welcome to France #{title}
  button(type="button", class="btn btn-secondary") I am a button 
  //- button(type="button", class="btn btn-secondary") Press me
